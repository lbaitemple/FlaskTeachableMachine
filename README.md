# FlaskTeachableMachine
Teablemachine flask implementation

```
curl -u username:passwd -F "picture=@name_pci.jpg" -F "model=@keras_model.h5" -F "class=@labels.txt" -F "format=json" http://hostname:8080/upload
```
